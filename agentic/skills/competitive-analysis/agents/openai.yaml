interface:
  display_name: "Competitive Analysis"
  short_description: "Compares alternatives and sharpens position."
  default_prompt: "Use $competitive-analysis to compare alternatives, gaps, and positioning before scope decisions."
dependencies:
  tools:
    - type: "mcp"
      value: "context7"
      description: "Context7 MCP for current library, framework, platform, API, CLI, and tool documentation"
