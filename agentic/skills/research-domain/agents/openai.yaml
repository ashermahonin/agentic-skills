interface:
  display_name: "Domain Research"
  short_description: "Maps users, domain facts, and constraints."
  default_prompt: "Use $research-domain to build the domain context and assumptions before requirements or architecture."
dependencies:
  tools:
    - type: "mcp"
      value: "context7"
      description: "Context7 MCP for current library, framework, platform, API, CLI, and tool documentation"
