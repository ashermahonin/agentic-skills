interface:
  display_name: "PR Review"
  short_description: "Finds regressions before changes land."
  default_prompt: "Use $pr-review to review a diff for correctness, regressions, missing tests, security, compatibility, and docs drift."
