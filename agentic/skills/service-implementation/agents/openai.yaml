interface:
  display_name: "Service Implementation"
  short_description: "Implements bounded tasks with validation."
  default_prompt: "Use $service-implementation to implement a bounded module or service after scope, ownership, contracts, and validation are clear."
dependencies:
  tools:
    - type: "mcp"
      value: "context7"
      description: "Context7 MCP for current library, framework, platform, API, CLI, and tool documentation"
